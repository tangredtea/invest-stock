# Required: at least 32 characters, and do not use known insecure defaults.
INVEST_JWT_SECRET=replace-with-a-random-secret-at-least-32-characters

# Optional web bind address. Default: :8081
INVEST_ADDR=:8081

# Optional K-line cache TTL. Valid Go duration, clamped to [1s,24h]. Default: 24h
INVEST_KLINE_TTL=24h

# Optional initial admin seed. If either value is invalid, seeding is skipped.
INVEST_ADMIN_USERNAME=admin
INVEST_ADMIN_PASSWORD=replace-with-a-strong-password
